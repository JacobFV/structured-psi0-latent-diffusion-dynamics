# project status

State: not started. This is a template, not a report of work performed.

Record the resolved repository path, validated peer alias, resource limits and proof files, current local commit, last verified milestone, active owned job/lease IDs, remaining CPU/GPU/time budgets, current experiment and raw artifact paths, exact next command, and external blockers here. Update at every handoff/checkpoint. Never include credentials.
