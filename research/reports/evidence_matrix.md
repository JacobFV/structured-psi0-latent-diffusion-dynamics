# evidence matrix (current; updated 2026-09-25 19:20 PDT, demo sprint; owner during the sprint: sprint_demo)

The single place that states what has and has not been shown. Every cell is backed by a saved raw output (paths
given) or says **not shown**. Column meanings:
- **implementation**: code exists and a smoke run of the real pipeline passed.
- **oracle-target behaviour**: packets made by the frozen encoder from the TEACHER's demonstrated actions (an oracle
  diagnostic that uses future actions; not deployable).
- **generated-packet behaviour**: packets sampled by system i from public observations (the deployable route).
- **semantic interventions**: VALID edits of the packet (binding, manipulator, requested effect) with irrelevant-edit
  controls. Zero/shuffle sensitivity and probe accuracy do not count as semantic control.
- **held-out transfer**: target bodies of the sealed protocol (xarm7_pg2, xarm7_tf3, panda_tf3).

Headline (23:30): **the deployable latent route (R2) now succeeds sometimes but stays well below plain BC: best 6/30 panda_pg2, 12/30 parm6_tf3 (flow_jointfix final → system 0 gendag2_noqd) vs BC 30/30, 30/30 on the same matched seeds. A semantic advantage of the packet is not shown (D-059, D-062).** The stateless oracle diagnostic (packets encoded from BC's own chunks) reaches 21/30 and 27/30: the BC→oracle gap is system 0's, the oracle→R2 gap the generator's. Fixes that moved the numbers: B-1 (D-044/45), BC-expert DAgger (D-053/55), removing the joint-velocity shortcut (D-056), DAgger on generated-packet states (D-063).

**D-074 (arm central-claim result, deployable route):** learned:ladder_flow_jointfix/snap_final_s20000 → system 0 ladder_rz_jointfix_gendag1_noqd on parm6_tf3, 41 dev seeds, edits of the public task context only: goal edit puts the cube at the new goal 15/41 vs 0–1/41 under three matched controls (irrelevant, probe-orthogonal, noise replay); descriptor-only rebinding: original cube lifted 0/41 (30/41 unedited), first approach to the new cube 31/41 (0/41), closest-approach preference +0.21 m [0.18, 0.25] beyond the irrelevant edit, new cube lifted only 5/41. Plain BC ignores the same rebinding (0/32). Caveats: one body (panda not competent on this route); the flow may read the binding via public predicate estimates; no nosem counterpart, so semantic supervision's role is not isolated; the rebound task is rarely completed (`artifacts/runs/acceptance_sprint_sem_gen_jf_parm6/semantic_summary_generated.json`).

**SPRINT BEST ROUTE FINAL (ladder.md, frozen 2026-09-26 01:10; D-072):** R2 learned:ladder_flow_jointfix_gdag1 → system 0 learned:ladder_rz_jointfix_gendag3_noqd (no teacher/oracle/BC at run time): panda_pg2 10/30 on the matched dev seeds, fresh sets 13/30 and 9/30 (BC u12000 on the same fresh sets 25/30 and 24/30), pooled 32/90; parm6_tf3 22/30, fresh 21/30 and 17/30 (BC 27/30 and 27/30), pooled 60/90; held-out source bodies parm5s_tf3 21/30 and parm5l_pg2 22/30 (BC u12000 26/30 and 29/30, teacher 30/30 each); 13 source-training bodies 209/312 (DAgger round-2 collection rollouts, not a held-out set). Raw: `artifacts/runs/ladder_v1/<robot>/generated_zero_flowgdag1_rzgendag3_noqd[_fresh<seed>].summary.json`, `learned_bc_direct1701_u12000_fresh<seed>.summary.json`, `artifacts/runs/ladder_dagger_gdag2/`. Demo page: `docs/demo/index.html` (built by `scripts/demo/build_page.py` from raw outputs).
The priority is a competent source controller and causal packet semantics
(research/corrections/2026-09-25-causal-semantics-priorities.md).


Sprint semantic results (sprint_semantic, acceptance.md "SPRINT SEMANTIC RESULTS"; panda_pg2, dev seeds): paired scenes where only the binding (entity descriptor) changes — teacher first touch on the rebound object 29/30 (control 0/30), goal edit 30/30, dual-arm arm swap 24/24; ORACLE DIAGNOSTIC on the B-1-fixed jointfix bundle: first touch new 19/30 (control 5/30), rebind minus irrelevant min-distance preference +0.156 m [0.115, 0.205], goal 0/30 — weak evidence, the packet encodes the teacher's demo toward the new object; competent BC reference (not latent): ignores a pure descriptor rebind (0/32, +0.006 m) but follows a belief swap (32/32) and a goal edit (24/32). Raw: `artifacts/runs/acceptance_sprint_sem_{teacher_paired,b1fix_oracle,bc_pp}/`, `artifacts/runs/acceptance_sprint_arm_teacher/`. Binding v4 sem vs nosem on the oracle route (D-059; `artifacts/runs/acceptance_sprint_sem_v4{sem,nosem}_oracle/`): rebind first touch new 8/30 vs control 5/30 (sem) and 4/30 vs 7/30 (nosem); rebind minus irrelevant +0.037 m [−0.001, +0.077] (sem) vs +0.052 [+0.020, +0.082] (nosem); the sem effect is not separable from a matched probe-orthogonal edit; neither v4 system 0 is competent before refitting. **No semantic advantage shown.** Best latent route (D-062; ORACLE DIAGNOSTIC packet = E(BC chunk for the edited context) → system 0 jfbcdag2, panda_pg2, 48 seeds, control 27/48; `artifacts/runs/acceptance_sprint_sem_best_orcbc/`): system 0 causally executes goal content carried in the packet — goal edit puts the cube at the new goal 16/48 vs 0/48 under both matched controls (+10.4 cm [8.1, 12.7] beyond the irrelevant edit); a probe-orthogonal edit drops success to 2/48; a binding edit is NOT followed (4/48). This does NOT show that semantic supervision adds control: BC already follows goal edits and the packet encodes BC's chunk; bindings are not carried because BC ignores them. Generated-route (v4 flows) semantic tests: running. **Causal packet semantics on a deployable route: still not shown.**

| component | implementation | oracle-target behaviour | generated-packet behaviour | semantic interventions | held-out transfer |
|---|---|---|---|---|---|
| single-arm pick_place, corrected path (latent_sem) | verified: stage A, stage B (standardized, resumable), system 0 runtime, eval, disturbance, render (D-031) | Probes on encoded z, training distribution: held_by_pos 0.956, rel_pos 0.054 m, subtask 1.00; shuffled z 0.151 / 0.365 m / 0.645; metadata-only 0.000 / 0.268 m / 0.784 (`artifacts/runs/latent_*_v1/probe_*.json`). System-0 1-step teacher-forced MSE 0.0129 vs zero-action 0.213. **Closed-loop oracle route FAILS: 0/8 on panda_pg2, never grasps**, while the scripted teacher succeeds 12/12 on the same seeds (`artifacts/runs/acceptance_semantic_{teacher,sem_v1rep}/`). Root cause B-1 (D-044). With B-1 fixed, system-0 refits on the frozen v1 encoder (refit, DAgger 1/2, anchored) are still 0/30 on panda_pg2 and parm6_tf3 (D-046): closed-loop compounding. A joint Stage-A retrain with the fix (D-047) is still 0/30 on the oracle route, but it now reaches the cube (0.3–4 cm) and fails at grasp/lift: system 0 does not follow a 'hover at pregrasp' packet precisely. DAgger round 1 on that jointly trained bundle: re-anchored R1 **1/30** on panda_pg2 (Wilson 0.006–0.17), 0/30 on parm6_tf3 (D-048; `ladder_v1/<robot>/oracle_zero_jfdag1_reanchor.summary.json`). Validity (D-049): re-anchoring the oracle expert every 8 ticks is valid (the teacher itself stays 20/20 on both bodies, `lead_teacher_reanchor/*_K8.json`); re-anchoring every tick is not (0/20, `*_K1.json`), so the `*_replan1` R1 runs are withdrawn. Observation (not a cause): in the valid runs the tool's median closest approach is 4–6 cm from the cube rather than the pregrasp waypoint 13 cm above it. **R1 is confounded** (sprint_bc, baselines.md): the shadow teacher is stateful and not a valid expert on learner-visited states — in the 25 panda_pg2 BC successes its final phase is still descend 11 / pregrasp 10 / lift 3 / transport 1 (`artifacts/runs/baselines_bc_ladder/<robot>/learned_direct1701_u12000.jsonl`, field final_teacher_phase), so oracle packets and shadow-DAgger labels can be stale; R1's 0–1/30 cannot separate 'system 0 cannot realize packets' from 'oracle packets are stale'. The latent failure is NOT localized. Clean test: R2 (system i's own packet, B-1-fixed flow) vs BC on the same seeds. Earlier symptom: at step 0 the packet norm is 98.6 (20–73 later) and system 0 outputs ≈−0.1 where the teacher commands −1.0; the arm barely moves and never recovers. Ladder track is localizing it. | Closed loop panda_pg2, dev seeds: flow v1 interrupted **0/64**; flow sem_v2 snapshot @22k **0/32**, never grasps, min TCP-cube 0.43 m (`artifacts/runs/grpo_latent_ref_*`). With a labelled 40-tick teacher prefix: 1/32 success, 12/32 grasps (not deployable). Full sem_v2 / nosem_v2 / sem_v3 dev evals: running (lead chain, host). **After the B-1 fix (the clean test, R2 vs BC on the same 30 matched seeds):** learned:ladder_flow_jointfix@4000 (flow with zero_prev_action on the jointly trained encoder, early snapshot of 20k) → jointfix system 0: 0/30 panda_pg2 (approach 16, grasp 11, lift 2, place 1; min TCP-cube 4.0 cm), 0/30 parm6_tf3 (approach 21, grasp 7, transport 2), vs plain BC 25/30 and 27/30 (`ladder_v1/<robot>/generated_zero_flowjf_s4000.summary.json`; later snapshots: sprint_latent watcher). | Counterexample (object rebinding) **FAILS**: 0/14 focus changes (D-032). The test had limitations (asymmetric graph edit, slot prior: metadata-only probe already gets focus 0.927); binding track is repairing it (symmetric rebinding, permutations, balanced bindings). Embodiment swap pg2↔tf3 on encoded z: focus agreement 0.98, subtask 1.00. Semantic edits (`rrp latent semantic-edits`): scripted teacher 12/12 for rebind_obj, goal_shift and both irrelevant controls, so the edits are physically achievable. Oracle route: rebinding moves the arm 8.4 [6.8, 9.7] cm from control vs 1.8/0.9 cm for irrelevant edits, but toward the ORIGINAL cube (−1.3 cm): packet dependence, not semantic control. Causal edits on the v1-interrupted generator: the packet matters (zero packet moves TCP 5.85 cm in 0.4 s vs 2.92 cm control), but directed edits are noise-level (rel +x: +0.03 cm) → **no semantic control shown**. | **not shown** (sealed; no competent source controller) |
| latent_nosem (capacity-matched control) | verified | Probes: held_by_pos 0.888, rel_pos 0.258 m (≈ metadata only) | nosem_v2 training | counterexample 0/14 (same limitations) | not shown |
| baseline_direct_action / action_only_codec | verified runner (`rrp campaign baseline-cell`, exact resume; D-035); ladder route `learned` | n/a | **plain BC with the B-1 fix is competent (final, seed 1701)**: direct 30/30 panda_pg2, 30/30 parm6_tf3 on the matched ladder seeds; held-out source bodies 79/80 (direct) / 77/80 (codec) (`artifacts/runs/latent_slice1_b1fix/<method>/seed1701/eval/source.summary.json`, `artifacts/runs/baselines_bc_ladder/`). Failures of the pre-fix latent path are not due to data, demonstrations or evaluator. | n/a | **budget-0 zero-shot to the sealed NEW bodies**: panda_tf3 78/100 (direct) / 85/100 (codec); xarm7_pg2 0/100 and xarm7_tf3 0/100 for both (`eval/<target>_b0.summary.json`; research/reports/latent_slice1_b1fix_baselines_tables.md). Latent methods not run on targets. |
| packet-policy GRPO | verified code + likelihood tests (`tests/unit/test_latent_grpo.py`) | n/a | dev (panda_pg2, base sem_v2 @22k): 0/64 from reset before and after 15 iterations; with the labelled 40-tick teacher prefix 0/64 → 1/64 (no improvement; D-042). On hold until a source policy is competent | n/a | not started |
| dual-arm (support_insert, handover), M=2 | verified pack/train smoke on 2 assemblies | teacher reference (scripted_teacher): support_insert 20/20, 19/20, 20/20, held-out source pair 9/17; handover 20/20 ×3, 16/16 | **not shown** (training deferred, D-043) | paired arm-assignment tasks ready: 2,203 valid identical-scene pairs; scripted teacher performs both assignments (D-043); learned: not shown | not shown |
| legged / humanoid | verified on go2 (D-060); teacher refs for 10 bodies (bodies.md) | go2 stateless oracle (packet = E(BC chunk)): nosem 30/30, sem 25/30 | **go2 deployable R2 competent: nosem 30/30, sem 29/30 vs BC 30/30, teacher 30/30** (`artifacts/runs/legged_ladder/go2/r2_*_snap_s4000.jsonl`, legged_vlm.md row 6b); BC: hexapod6 30/30 (hexapod6 stateless oracle 30/30 sem and nosem), t1 24/30, **g1 BC positive control fails (2–7/30 vs teacher 25/30)** | D-069: probe-guided halt −1.24/−1.23 m (sem/nosem) vs −0.05..−0.19 m random; yaw ±0.6 sign-correct 0.16–0.30 rad vs ≈0; goal-mirror null; contact weak — packet→behaviour causality along probe directions on an oracle route; no sem advantage. **D-071 (deployable route, task-context edit): mirroring the active waypoint moves go2 +0.355 m [0.22, 0.50] (sem) / +0.43 m [0.30, 0.59] (nosem) toward the mirrored side vs +0.04 m for the inactive-waypoint control — task → packet → behaviour for goal direction, one body, one semantic; halt via context fails (OOD); sem-specific handle small (6–7 cm)** (`artifacts/runs/legged_edits/go2/r2ctx_{sem,nosem}_snap_s4000/mirror_effects.json`) | not shown |
| VLM system II | smoke (Qwen3-VL weights hash-verified, isolated packages) | n/a | **not shown** | not shown | not shown |
| latency | verified (`rrp latent latency --direct-config`) | — | **clean (D-058): p95 28.7 ms latent (learned:ladder_flow_jointfix 20k → system 0) vs 28.3 ms real B-1-fixed BC direct1701_u18000, overhead 1.014× (limit 1.25×), system-0 tick p95 1.7 ms, 0 deadline misses; quiet GPU, 100 interleaved pairs, NFE 8** (`artifacts/runs/lead_latency_final/flowjf20k_vs_direct18k.json`). Supersedes the D-041 1.12× estimate (random direct weights, loaded peer). | — | — |
| compatibility / safety | verified: bundle fingerprint in compatibility IDs with a mismatch-rejection test (D-038); packet admission, staleness, spec checks | — | — | — | — |

## in flight on the critical path
1. ladder track: R0 expert → tracker, R1 oracle packet → system 0, R2 generated packet → system 0, on matched scenes (item 3).
2. binding track: repaired counterexample, then paired physically consistent tasks through the whole pipeline, plus goal/predicted/observed-effect separation (items 1, 2, 5).
3. acceptance track: valid semantic interventions plus irrelevant-edit controls, once a competent checkpoint exists (item 4).
4. lead chain (host GPU): flow sem_v2 → nosem_v2 → sem_v3, each with a 20-episode dev eval on 4 source bodies, a disturbance test and videos.
