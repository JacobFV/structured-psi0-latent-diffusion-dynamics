# decision log

Append-only. Each entry: prior assumption, evidence, decision, expected effect on claims.

## D-001 2026-09-21 repository location
`~/work/relational-robot-policy` did not exist; created fresh. Handoff preserved verbatim in `docs/handoff/` (SHA256SUMS verified OK).

## D-002 2026-09-21 peer identity (R02)
Candidates from literal `~/.ssh/config` aliases matching gb10/spark (no scanning, BatchMode, StrictHostKeyChecking=yes). Receipt: `artifacts/receipts/ops/peer-discovery.json`.
- `gb10-direct` -> hostname promaxgb10-4dfb, aarch64, NVIDIA GB10 driver 580.126.09, 20 cores, ~115 GiB available, no GPU compute processes, route over the physical direct link (enp1s0f1np1 10.100.216.0/24). `promaxgb10-4dfb` alias is the same machine via Tailscale (same machine-id).
- `spark-ec4d` is a THIRD GB10 (distinct machine-id) running a foreign vLLM EngineCore (92 GB); it is not the physically connected peer and is not used.
- `spark-gb10` unreachable (no route).
Decision: peer = `gb10-direct`. Local notes (`~/SSH-REMOTE-DIRECTIONS.md`) predate the direct link and were not trusted over live identity checks.

## D-003 2026-09-21 peer persistent disk is effectively unavailable
Peer root FS: 1.9 TiB, 188 GiB free (90% used). Reserve = max(20 GiB, 10% capacity) ≈ 186 GiB -> policy allows ~0 new persistent bytes. Decision: all peer project files (venv, caches, datasets, checkpoints) live in RAM-backed `/dev/shm/rrp-brandonin` (tmpfs; charged to our memory cgroup, so inside the peer memory budget). Durable artifacts are pulled to the host (host new-disk budget ~59 GiB). Consequence: a peer reboot loses the peer workspace; everything must be reproducible from the host repo + lockfiles, and checkpoints are synced back after each training segment. This tmpfs must be removed at final shutdown or listed as retained.

## D-004 2026-09-21 GPU memory is not visible to memcg on GB10 (R03)
Measured on peer (`gpu_memcg_probe`): a 3 GiB CUDA allocation inside a lease with MemoryMax=2 GiB succeeded; lease memory.current barely changed. `torch.cuda.mem_get_info` reported only ~6 GiB "free" (MemFree, excluding reclaimable page cache) of 119.6 GiB. Decision: leases carry a declared `gpu_memory_bytes` that is added to the aggregate memory sum; every GPU workload calls `rrp.ops.gpu.apply_cap()` (torch per-process memory fraction; tested: allocation beyond cap raises OOM in-process); system MemAvailable watchdog is the backstop. Host GPU remains disabled: cgroups cannot bound it.

## D-005 2026-09-21 host state at admission
Host had swap fully used (16 GiB) by other workloads and ~40-47 GiB available at admission, load ~4.6. Prior OOM incident on 2026-09-13 noted (`~/GB10-OOM-NOTE-2026-09-13.md`), probably a GPU workload. Host parent quota: CPUQuota 7.11 cores, MemoryMax 20.0 GiB, MemoryHigh 0.8x, swap 0, TasksMax 4096 (`configs/resources.local.json`). Watchdog lowers CPU quota live and revokes newest leases when the live memory limit falls below leased totals.

## D-006 2026-09-21 MemoryHigh behaviour
A child exceeding MemoryHigh with no swap stalls in reclaim (PSI full ~89%) instead of dying. The watchdog therefore sheds on sustained project memory PSI (tested with a bounded fixture in `tests/integration/test_resource_enforcement.py`).

## D-007 2026-09-21 INCIDENT: brief unintended host GPU use; fixed with hard device exclusion
A 128x96 MuJoCo render probe (20 frames, <1 s, lease 1790017490_e25c21) created an EGL context on the host NVIDIA GB10 because the lease runner's environment allowlist dropped the Mesa vendor override and `CUDA_VISIBLE_DEVICES=""` does not affect EGL. No other workload was affected (probe finished in <1 s), but it violated the host-GPU-off rule. Fix: every non-GPU lease now runs with systemd `PrivateDevices=yes` (verified: /dev/nvidia* absent inside the unit) and forced Mesa llvmpipe EGL env; host GPU leases are refused in code. Regression test: `tests/integration/test_host_gpu_exclusion.py`. Host software rendering measured at ~18 fps for 128x96 on one core, so data rendering belongs on the peer.

## D-008 2026-09-21 user correction: the whole peer may be used
User instruction (supersedes the handoff's 80%-of-free peer default): "you can use all the peer". Peer policy now: 100% of free CPU/RAM/disk minus a small OS safety reserve (6 GiB RAM, 10 GiB disk); system-wide PSI shedding threshold on the peer raised to 80 (project-level stalls still shed). Persistent peer disk (~178 GiB usable) may now be used, but the RAM-backed workspace remains for speed; durable artifacts still sync to the host. Host limits (<=50% of currently free, no host GPU) are unchanged.

## D-009 2026-09-21 peer GPU concurrency 3
With the whole peer authorized (D-008) and five parallel engineering tracks, the peer broker admits up to 3 concurrent GPU leases. Each declares gpu_memory_bytes (counted in the aggregate memory limit) and applies the in-process CUDA cap; the system MemAvailable watchdog remains the backstop. Timing/latency measurements must be taken with no other GPU lease active (recorded per measurement).

## D-010 2026-09-21 thermal guard based on throttling evidence
The initial 95 C CPU shed threshold repeatedly killed healthy peer jobs (evidence: ops/watchdog/peer.jsonl, leases 1790033520_d98918, 1790038081_a0568e). GB10 exposes no thermal trip points; at 96-97 C the CPUs ran at full 2.808 GHz and GPU thermal-slowdown flags were inactive. New rule: shed on GPU HW/SW thermal slowdown, on CPU >=90 C with clocks below 70% of max, or at >=100 C; stop admission (no new leases) at >=97 C. Killed runs are preserved and rerun from checkpoints.

## D-011 2026-09-21 GPU memory attribution in the live limit
Dev runs 1790038081_07a6c7 (structured, interrupted at step 6057/epoch 3) and 1790045747_76b6bd (unstructured, step 1) were revoked with `live_limit_reduced`: CUDA allocations lower MemAvailable but are not in memcg, so the live limit treated our own GPU memory as external load. Fix: project residency P now = memcg memory.current + nvidia-smi used_memory of processes inside rrp.slice. Interrupted runs preserved.

## D-012 2026-09-21 development recipe intervention #1 for BC competence: kinematic features (feat-v2)
Evidence (dev only, eval scenes 3,000,000+): partial structured BC checkpoint succeeded 1/70 attempted dev episodes (artifacts/runs/dev_structured_direct/eval_dev.jsonl on peer). Open-loop chunk MSE on training robots: panda_pg2 0.094 (zero-action 0.47), parm6_pg2 0.59 (worse than hold-first 0.14); closed-loop panda traces descend to the table 20-30 cm from the cube => target localization in joint space failed. Change (applies identically to every method): per action node, the grasp-TCP Jacobian columns (position+rotation) and lever arm from the public kinematic model at measured q; per object slot, position relative to the TCP. Featurizer version feat-v2; dataset regenerated as pick_place_primary_v2 (same seeds/split). feat-v1 checkpoints are archived, not mixed with v2 results.

## D-013 2026-09-21 dual-arm runtime semantics needed by the supplied support_insert graph
Prior: the task runtime evaluated only `event_succeeded` among validated_receipt predicates; everything else was UNKNOWN. Evidence: in DualSession runs, `align` (precondition anchor_valid) and `insert` (precondition receipt_valid) stayed in `precondition_unknown` indefinitely; after a failed `align` attempt, `insert` kept requiring `align#0.aligned`, because rebinding was triggered only by role-binding consumers; unchanged maintained anchors were re-issued as a new receipt version on every tick (covariance was popped before storage but not before the comparison). Decision: `anchor_valid`/`receipt_valid`/`frame_valid` evaluate the latest bound receipt (missing = known-invalid); guard-condition and `frame_binding` consumers trigger recovery rebinding, and the rebind edit moves `frame_binding`; the maintained-output comparison uses the stored form. All are backwards compatible (the existing runtime/native/intervention tests pass). Effect on claims: before this change the supplied support_insert graph could not complete under any controller.

## D-014 2026-09-21 support_insert teacher eligibility (resolved per pair before any dataset generation)
Rule (research/splits/primary_v1_support_insert.json): a source pair is eligible if scripted_teacher success/feasible >= 0.5 over seeds 0-29 (artifacts/assets/dual_teacher_validation/support_insert_30seeds_v2.jsonl). Resolved for the pairs whose validation completed first: eligible parm5_pg2__parm5_pg2 (27/27 feasible), parm5_tf3__parm7_pg2 (21/23), parm6_tf3__parm6_pg2 (19/19), parm7_pg2__parm5_tf3 (19/27), panda_pg2__ur5e_pg2 (30/30), ur5e_pg2__sawyer_pg2 (27/30). Ineligible: parm6_pg2__parm7_tf3 (10/22; seven-joint tf3 insertion arm lets the peg tilt), sawyer_pg2__panda_pg2 (0/26; the sawyer support arm blocks the insertion arm's transit). The remaining pairs are resolved in D-017 once their validation finishes. Demonstrations are generated only for eligible source pairs: 150 per pair, seeds 0-149, in configs/data/support_insert_primary_v1.json. Failed and infeasible episodes are recorded, not dropped.

## D-017 2026-09-21 support_insert eligibility, remaining pairs (renumbered from a colliding D-015) (validation complete: support_insert_30seeds_final.jsonl, 16 pairs x 30 seeds)
Source pairs: sawyer_tf3__ur5e_pg2 0/30 and ur5e_tf3__parm6_pg2 0/19 feasible, so both are ineligible (a sawyer support arm blocks the insertion arm; the ur5e+tf3 support hand never produced a valid anchor, so `align` never activated). Final eligible source set (6 pairs): parm5_pg2__parm5_pg2, parm5_tf3__parm7_pg2, parm6_tf3__parm6_pg2, parm7_pg2__parm5_tf3, panda_pg2__ur5e_pg2, ur5e_pg2__sawyer_pg2. The source held-out evaluation pair parm5l_pg2__parm5s_tf3 scored 19/29. Target teacher rates, reported and not used for selection: xarm7_pg2__xarm7_pg2 30/30, xarm7_tf3__xarm7_pg2 30/30, ur5e_pg2__panda_tf3 3/30 and parm6_pg2__panda_tf3 11/30 (TEACHER-WEAK: the panda+tf3 insertion arm drops or tilts the peg in transit), aloha 22/30. Target adaptation demos (seeds 1,000,000+, 60 per target pair) and 60 development demos for the held-out source pair (seeds 3,000,000+) are added to configs/data/support_insert_primary_v1.json. Adaptation data for a teacher-weak target contains mostly failures and must be reported as such.

## D-015 2026-09-21 private remote authorized by user
User asked for a private repo. Created https://github.com/JacobFV/relational-robot-policy (PRIVATE) and pushed main. Excluded: tokens (ops/workbench-token), model checkpoints (*.pt except small project-trained tracker actors), third-party assets/weights (.cache/, psi0/Qwen weights never committed), datasets. Push regularly after local commits; never make it public without separate authorization.

## D-016 2026-09-21 teacher fix: continuous grasp yaw (data quality, not a model recipe change)
Action statistics (scripts/diag_actions.py) showed the last arm joint (wrist roll) with first-step normalized delta std 1.1-1.3 and peaks 5.8-11.6 (up to ~6 rad) on procedural arms: the scripted teacher chose the cube-symmetric grasp yaw in [-pi/4, pi/4] regardless of the current tool yaw, producing wrist flips that are not inferable from the state. Fix: choose the symmetric yaw nearest the current tool yaw. Teacher re-validated and dataset regenerated (pick_place_primary_v3); earlier datasets archived.

## D-018 2026-09-21 support_insert_primary_v1 dataset; known public-estimator limitation (renumbered from a colliding D-016)
Generated on the peer (1260 episodes: 910 success / 231 failure / 119 infeasible; manifest_hash 2181bf6728ab7a6d) and pulled to artifacts/datasets/support_insert_primary_v1 (1.1 GB). The CPython 3.12 ProcessPoolExecutor hung twice when workers were recycled (max_tasks_per_child): the workers disappeared and the parent blocked on a futex. Collection therefore resumes in the parent and does not recycle workers; memory is bounded by gc after each episode. Limitation, not fixed in v1: the public insertion depth combines FK with a lagged in-hand belief. When a peg jams and slips in the grasp, it can report a successful insertion that did not happen. Measured per pair as `agree` in the validation tables (pg2 insertion arms 30/30; three-finger insertion arms 17-26/30), and it shows up in the functional-composition `shifted` condition. Dataset `status` always comes from the privileged evaluator, so demonstrations are not mislabelled; `public_runtime_success` is stored separately. A v2 estimator (take the conservative minimum of the depth from direct detection when the peg is visible and the FK-based depth) is proposed and not implemented, so that this dataset keeps a single estimator version.

## D-019 2026-09-21 dual teacher v2 (orientation continuity), re-validation, dataset v2
The lead flagged wrist flips in the pick_place teacher (their D-016). The dual teachers had the same class of problem. Measured on support_insert_primary_v1: normalized single-step arm actions up to 3.14 at the first steps on procedural pairs (a pi/2 wrist snap to the fixed grasp yaw), 6.28 on ALOHA/panda, and 11.6 later on panda+tf3 (IK reseed jumps). Fix, applied to every dual teacher: the commanded tool orientation starts at the CURRENT tool pose, heads for the nearest symmetric equivalent of the target (pi for parallel jaws, 2pi/3 for three-finger hands) at <= 1.5 rad/s, and joint targets are slewed at <= 3 rad/s. Smoke check: max |a| 0.3 (parm5), 1.31 (panda/ur5e, joint tracking lag), 1.23 (ALOHA); every smoke episode succeeded. Re-validated with 30 seeds per pair (support_insert_30seeds_v3.jsonl, handover_30seeds_v3.jsonl). Eligibility re-resolved with the unchanged rule: parm6_pg2__parm7_tf3 (15/22) and sawyer_tf3__ur5e_pg2 (22/30) became eligible; sawyer_pg2__panda_pg2 (1/30) and ur5e_tf3__parm6_pg2 (0/19) stay ineligible. ALOHA support_insert went from 22/30 to 30/30. The v1 validation files and the dataset artifacts/datasets/support_insert_primary_v1 (teacher v1) are superseded and archived: do not train on them together with v2 data. New dataset: configs/data/support_insert_primary_v2.json (8 source pairs x 150, 60 held-out-source development episodes, 5 targets x 60).

## D-020 2026-09-21 BUG: stale featurizer cache in LearnedPolicy (invalidates earlier closed-loop evals)
LearnedPolicy cached featurizers and previous actions by id(session). Garbage-collected sessions' ids were reused by new sessions (including other robots), so some episodes were featurized with a stale featurizer (wrong kinematics/prev action); one run crashed with KeyError('r0_joint6'). All closed-loop evaluations before this fix (dev_structured_partial, dev4_*) are INVALID and kept only as bug evidence. Fix: featurizer/prev-action stored on the session object.

## D-021 2026-09-21 development recipe intervention #2: remove previous-action input
After fixing D-020, dev4 policies still reached 1/71 successes in closed loop on dev scenes (both methods identical; artifacts/runs/dev4_*/eval_dev.jsonl on peer) despite open-loop chunk MSE well below hold/zero baselines (panda 0.058 vs 0.148; parm6 0.048 vs 0.127). The previous-action node feature has different semantics in training (teacher's previous 1-step command) and inference (last executed row of the policy's own chunk, an 8-step delta) and permits copycat shortcuts. Removed for all methods (zeroed at load for stored datasets). Next fallback if still failing: DART data (recipe #3).

## D-022 2026-09-21 development recipe intervention #3 (last before analysis decision): DART data + 6 epochs
dev5 (D-021) dev-scene closed-loop successes (prefix 8): structured panda 2/20, parm6_tf3 4/13, parm5s_tf3 4/18, xarm7 0/20; unstructured 3/20, 0/13, 4/18, 0/20. Prefix 4 was worse for both. Recipe #3 for both methods: pick_place_primary_v3dart (clean + execution-noise demonstrations with clean teacher labels, noisy failures included as corrective labels) and 6 epochs. After this run an explicit analysis decision is recorded regardless of outcome.

## D-023 2026-09-21 humanoid breadth status (no retroactive gate change)
Legged track (research/reports/legged_breadth.md): procedural hexapod/quadrupeds and go2/anymal_c trackers pass their pre-declared controller gates; t1/g1/h1 trackers walk and arc without falls but FAIL the pre-declared in-place-turn criterion. We do not relax that gate after the fact: humanoids are reported as "limited qualification: forward/arc waypoint locomotion" and the >=3 humanoid-family breadth gate is recorded as NOT MET unless turning is fixed in a new, separately registered tracker iteration. psi0 36/80-dim contracts implemented; no body validated for them (menagerie G1/H1 lack the required hands) -> blocked_external for psi0-native control.

## D-024 2026-09-21 swap-growth guard latched permanently (fixed)
The peer watchdog compared SwapFree to its start-time baseline; swapped-out pages remain swapped even with ~96 GB available, so the guard stayed at "shed" and killed/refused all jobs (dev6_structured killed; GRPO track blocked). Now: growth measured over a ~60 s window; shedding additionally requires tight memory (MemAvailable < 3x reserve); otherwise only admission pauses until growth ages out.

## D-025 2026-09-21 disclosure: target body appeared in development evals
Development evaluations dev_structured_partial, dev4 and dev5 included xarm7_pg2 (a primary TARGET body) on development scenes (3,000,000+). All its results were 0-1/20 and no recipe choice depended on them (recipe changes D-012/D-016/D-021/D-022 were motivated by source-body diagnostics). From now on development evaluation uses only source-train bodies on new scenes and the held-out-source bodies (parm5s_tf3, parm5l_pg2); target bodies are evaluated only in the sealed protocol.

## D-026 2026-09-21 user instruction: no leases/limits on the peer
User: "there should be no leases / limits on the peer machine (unless you're doing that to share the machine with multiple of your own trains)". Peer now runs in unrestricted mode: broker is a job registry only (no capacity refusals), lease slices have CPUQuota/MemoryMax/MemoryHigh unset, watchdog acts only on emergencies (MemAvailable < 3 GiB, disk < 2 GiB, GPU thermal slowdown / CPU >= 100 C or clock throttling). Host limits unchanged (shared machine).

## D-027 2026-09-21 user authorized host GPU at 80% of free memory
User: "authorize host GPU at 80% of free memory". Host memory budget (CPU+GPU unified) = min(0.8 x MemAvailable, MemAvailable - reserve), live-updated; host CPU unchanged at 50% of idle cores; up to 2 concurrent host GPU leases; every GPU job applies the in-process CUDA cap to its declared GPU memory; the host watchdog (MemAvailable, PSI, swap, thermal incl. GPU slowdown) sheds owned jobs when other users need the machine. Non-GPU host leases still run with PrivateDevices. Host jobs are resumable, non-critical-path by preference.

## D-028 2026-09-21 BUG: broker held its lock across systemctl calls -> cascading lease expiry
At 08:33-08:35 stopping one slow job (TimeoutStopSec 45 s) inside the broker's state lock (release/_expire/gc called backend.remove_lease under flock) blocked every other job's heartbeat for >20 s; five healthy peer jobs (dev6 x2, codec, support_insert x2) expired and were stopped. Fix: all systemd calls happen after the lock is released; lease expiry raised to 120 s; regression test test_slow_backend_cleanup_never_blocks_heartbeats. Affected runs restarted.

## D-029 2026-09-21 authoritative correction: controller-facing semantic latent
User correction received; recorded in research/corrections/controller-facing-semantic-latent.md. No incompatible jobs were running (peer rebooted after the OOM hang; host queue empty); no old-path runs will be scheduled except as named baselines. Pre-correction revision tagged pre-correction-272c689; migration branch correction/controller-facing-latent. Peer-only raw eval rows of dev4/dev5 were lost in the reboot (summaries remain in D-021/D-022).

## D-030 2026-09-25 repository renamed
User renamed the GitHub repo to JacobFV/structured-psi0-latent-diffusion-dynamics and made it PUBLIC intentionally: the project is meant to be shared. Supersedes the private-only rule from the earlier repo-creation decision; still never commit tokens, weights, third-party assets or datasets. `origin` updated to the new URL. The local checkout path (~/work/relational-robot-policy), peer workspace, Python package `rrp` and systemd unit names are unchanged, so running jobs and resume steps are unaffected. docs/handoff/ is kept verbatim and still uses the old name.

## D-031 2026-09-25 stage-B loss gap explained; flow target standardized; v1 flows superseded
Probe analysis (frozen z, identical procedure; raw in artifacts/runs/latent_{sem,nosem}_v1/probe_*.json): semantic z is far more decodable than no-semantic z (held_by_pos 0.956 vs 0.888; acting_on_pos 0.871 vs 0.622; rel_pos err 0.054 m vs 0.258 m; subtask 1.00 vs 0.968). The metadata-only control (no z) reaches rel_pos 0.268 m and held_by_pos 0.0, so nosem z carries almost no relative-geometry information beyond metadata. Training-distribution, encoded-target numbers only.
Early flow-loss gap (4.47 vs 0.188 at step 1500) is mostly scale: encoder-mean second moment 5.24 (sem) vs 0.144 (nosem), median per-dim std 2.08 vs 0.27 (artifacts/runs/latent_scale_analysis.json). The flow regressed raw z, so the zero-knowledge MSE floor is E[z^2]+1 = 6.24 vs 1.14. Floor-normalized the gap shrinks but remains (0.72 vs 0.16 at 1500; 0.38 vs 0.09 at 4100), attributable to the packet-semantic term.
Changes: FlowPolicy standardizes its target per dim (buffers z_mean/z_std, identity by default; sample() and the semantic objective see raw z so system 0 receives the same packet space). train-flow now writes a full resume state (policy_last.pt: model, optimizer, OneCycle scheduler, RNG) every 1000 steps and resumes automatically with a config-equality check. The v1 interrupted checkpoints (no optimizer state, raw target) are NOT resumed; fresh runs flow_latent_{sem,nosem}_v2 in new directories.
Early v2 log shows the semantic term drives the standardized flow loss above the do-nothing value 2.0 (5.49 at step 500, 2.17 at 2000): demanding a confident clean packet from near-noise z_tau distorts the velocity field. Added packet_tau_min (semantic gradient only from samples with tau >= tau_min); flow_latent_sem_v3 = v2 + packet_tau_min 0.6. Chains are serial (foreground leases); latent_eval_chain.sh (detached jobs) is retired in favour of scripts/latent_chain_v2.sh.
Peer reboot fix: menagerie assets were in RAM (.cache under /dev/shm, never synced) and were lost; peer_bootstrap.sh now keeps them on disk (~/rrp-peer-data/cache, pinned SHA verified) and restores dataset/packed links.

## D-032 2026-09-25 acceptance: counterexample FAILS for both representations; embodiment swap passes
`rrp latent counterfactuals` (post-hoc probes for both variants; artifacts/runs/latent_*_v1/counterfactuals_posthoc.json; encoded teacher targets). Compatible embodiment swap (parm6 pg2 vs tf3, 51 phase-matched pairs): sem focus agreement 0.98, subtask 1.00, desired-delta disagreement 0.007 m; nosem 1.00 / 1.00 / 0.0004 m. Counterexample (same trajectory + body, task patient rebound to another object, 14 pairs): sem z moves 4.9% of its norm and the probed focus changes 0/14; nosem 1.8%, 0/14. The encoders read the task object from the demonstrated trajectory, not from the supplied task binding, so the packet does not yet carry context-dependent meaning that an action-only codec lacks. Stage A needs a binding-sensitive revision (e.g. counterfactual-binding augmentation/contrastive term) before the sealed four-way campaign. The joint P of latent_nosem_v1 is untrained by design, so the first counterfactuals.json for nosem is invalid; use the _posthoc file.

## D-033 2026-09-25 user authorization: host 80%, peer 100%, parallel agents, merge to main as you go
User: "use up to 80% of this host and 100% of the peer gb10. use subagents and keep the big picture in mind. commit and document as you go. try to merge things on main as you go." Host policy is now 80% of free CPU cores (was 50%) and 80% of free memory (unchanged since D-027), with reserves and the live watchdog; host GPU leases are allowed. Re-initialized: rrp.slice 11.12 CPU / 33.2 GiB at the time of measurement. The peer stays unrestricted, and GPU jobs may share the GB10 (the "serialize GPU jobs" handoff note applied to detached chains that hid failures; chains still run their own steps in order). Rebuilt the host .venv (it was a self-referencing symlink) with torch 2.14 cu130. Added scripts/peer_run.sh and RRP_PEER_REPO so each agent has its own peer code dir with shared artifacts and one broker. `main` is the integration branch.

## D-034 2026-09-25 host disk reserve hit; packed-data mirror removed
The host data mirror (~29 GB) plus other disk growth took host free disk to ~353 GiB, below the 10%-of-total reserve (~367 GiB). The watchdog stopped all host admissions (reported by the baselines track, which correctly left the reserve alone). The reserve protects other users of this shared disk and was NOT lowered. I deleted the host copy of artifacts/packed (24 GB; the peer copy is the source) and kept the datasets (4 GB). Free disk is 377 GiB and admission is ok, with only ~10 GB of headroom. Consequence: packed-data training (representations, flows, baseline sources) stays on the peer. The host serves CPU evals, rollouts and GPU work that needs no packed data. Host GPU training on packed data would require lowering the disk reserve, which is the user's call.

## D-035 2026-09-25 baselines track delivered the campaign runner; peer CPU priorities set
Baselines (research/tracks/baselines.md; merged 853b35d): `rrp campaign baseline-cell` runs one sealed latent_slice1 cell resumably (trains the source if missing, then SFT at budgets 0/5/20/100, then 100 episodes from seed 2,000,000 with Wilson CIs). Baseline SFT copies the latent cell's adaptation (same target packs/demos, 150/300/600 updates). Source-model resume is exact. The report script is `rrp.evaluation.latent_slice1_report`. The track fixed a latent bug before it bit: the old SFT would have trained the codec policy on raw actions. Six recorded deviations (implementation choices made before any result). Compute is not matched: ~26.3k×256 samples per baseline source vs 30k×128 for the latent flows; this is reported, not changed. No baseline numbers yet: the direct and codec seed-1701 sources are training on the peer (leases aea797, 518d74, host supervisors). On the oversubscribed peer each source takes ~8–14 h, so the baseline grid needs ~1–2 days.
Peer contention (12 jobs, load ~42 on 20 cores): set cgroup CPUWeight at runtime. Critical path (flow_latent_sem_v2, binding_rep_sem/nosem) = 400. Dev references on the interrupted v1 checkpoint, the v1 re-eval and legged data collection = 25. Everything else = 100. Reversible; nothing was stopped.

## D-036 2026-09-25 user set the host disk reserve to 300 GB; transfers reserve disk
User: "yes, lower the disk reserve to 300 GB". The host RolePolicy disk reserve is now a fixed 300 GB (was max(20 GB, 10% of 3.6 TB) ≈ 367 GB). Re-initialized: rrp.slice 14.97 CPU / 34.2 GiB; the new-disk budget is 78 GB. `rrp ops run --disk <size>` now reserves disk in the broker, so downloads and transfers declare their size up front. Mother-agent feedback item 7 asked for disk to be reserved before any download. The packed mirror (24 GB) is being restored under a 26 GB reservation (label mirror_packed), which re-enables host GPU training on packed data.

## D-037 2026-09-25 mother-agent priorities adopted; tracks re-scoped
Authoritative correction: research/corrections/2026-09-25-causal-semantics-priorities.md (the user relayed the mother-agent feedback). Owners: binding owns items 1, 2 (object binding through the WHOLE pipeline: paired physically-consistent tasks with valid demos, same-trajectory/different-meaning set kept separate with no contradictory action labels) and 5 (goal_effect / predicted_effect / observed_effect). dualarm is re-scoped to paired manipulator-assignment tasks for item 2. A new ladder track owns item 3: expert → tracker, expert-encoded packet (ORACLE) → system 0, generated packet → system 0, on matched scenes. acceptance is re-scoped to item 4 (valid semantic edits plus irrelevant-edit controls on a competent checkpoint). The lead owns items 6 and 7. Expansion held: the baselines sweep is limited to seed 1701 (sources, source competence, budget-0 cells; D-035 follow-up at 2932a51), GRPO starts no new runs, legged/VLM winds down. All earlier runs, failed runs and resource ledgers are preserved.

## D-038 2026-09-25 compatibility IDs fingerprint the frozen bundle (item 6)
Before: latent_space_version was a hash of the LatentConfig only, and LatentSystem0 took its expected IDs from the generator's own claim, so a retrained encoder or realizer with the same config (exactly what the binding track produces) would have been accepted silently. Now: latent_space_version = <config hash>-w<sha256 of encoder weights>, and realizer_compat_version adds the realizer weights hash. load_representation stamps these on the realizer. LatentSystem0 uses its realizer's own fingerprint, and LatentPolicy.from_checkpoint rejects (latent_space_mismatch) a fingerprinted generator whose bundle no longer matches its representation file. Pre-D-038 generators load with legacy_unfingerprinted=True, bound to the representation FILE: never overwrite a representation.pt in place. Test: tests/unit/test_latent_contract.py::test_bundle_fingerprint_rejects_same_config_retrained_bundle. Peer smoke: flow_latent_sem_v1 loads as legacy with ID ls-8db814f941f5-wd246dff9b00b, which matches the realizer; the sem and nosem bundles differ.

## D-039 2026-09-25 lead chain moved to the host GPU; peripheral GPU jobs cleared; evidence matrix
With 9 processes time-sliced on the peer GB10, flow_latent_sem_v2 fell to 0.3 steps/s. It was stopped with a scoped lease stop (exact snapshot at step 24,543) and resumed on the idle host GPU via the now node-agnostic scripts/latent_chain_v2.sh (unit rrp-chain-host). The acceptance-track v1-interrupted reference runs and the legged representation/collection jobs were asked to stop (peripheral, D-037). Restored the host packed mirror under a disk reservation; it grew to 38 GB because the peer now also holds the 14 GB dual-arm pack; free disk 340 GB > 300 GB reserve. Fetched the pinned menagerie on the host. Item 7: research/reports/evidence_matrix.md is the single current evidence statement; README and STATUS are reconciled with it (branches, resource authorization, resume commands). Earlier accounting and failed runs are kept.

## D-040 2026-09-25 legged/VLM track wound down (peripheral, D-037); incident: unreserved copy tripped the disk reserve
Delivered (merged 0c19e57; research/tracks/legged_vlm.md): a legged latent-packet path with one assembly per leg plus body and arms; packet semantics = per-leg foot contact per knot, goal waypoint, base displacement, subtask, fall; a 50 Hz system 0, the flow and a closed-loop eval with causal packet edits; 3 leakage tests pass. Teacher data: 600 episodes each for 8 bodies (anymal_c not collected); teacher reference 20/20 on six bodies (dev seeds 10000-10019). Stopped by the lead: the representation runs at 9.2k/9.4k of 12k, lost because that trainer has no periodic checkpoint (to add before rerunning). The no-semantic packet was near collapse in training batches (unverified). VLM system II (psi0) code is written but never ran; its grounding smoke was stopped under peer memory pressure; it is UNVERIFIED on main. No learned legged or VLM result exists. The tracker weights for anymal_c/g1/h1/t1 were recovered from ~/.archive/relational-robot-policy-2026-09-25-original (not committed). Fixed: peer_sync deleted worktree artifacts symlinks on push (excludes now anchored).
Incident: the track copied 4 GB of psi0 weights to the host without a disk reservation. That took free disk below the then-reserve (~367 GB), and the host watchdog killed binding_v1_reeval (lease 1790363904_b7ef3b, 31 min in); the binding track was asked to relaunch it. The copy was deleted. Rule (D-036): every download or transfer declares `--disk`.

## D-041 2026-09-25 acceptance suite delivered; the oracle route also fails closed loop
Acceptance track (research/tracks/acceptance.md; merged on main): `rrp latent semantic-edits --route teacher|oracle|generated` (rebind_obj, goal_shift, and controls irrelevant_distractor / orthogonal_matched; edits change the context the packet is generated for, never the physical scene), `rrp latent causal`, `rrp latent composition`, `rrp latent latency --direct-config`, scripts/render_causal_edit.py. Results: the scripted teacher does every edit (12/12 each), so the edits are achievable. The ORACLE route (teacher-encoded packet → frozen system 0; latent_sem_v1) succeeds 0/8 and never grasps. Rebinding moves the arm 8.4 cm vs 0.9–1.8 cm for irrelevant edits, but in the wrong direction: packet dependence without semantic control. Suspected cause: episode start (packet norm 98.6 at step 0; system 0 outputs ≈−0.1 vs the teacher's −1.0); passed to the ladder track. Causal edits on the v1-interrupted generator: zero packet 5.85 cm vs control 2.92 cm; 5 cm geometry edits steer 0.01–0.06 cm. Latency p95 1.12× (threshold 1.25×) with caveats (direct path timed with random weights, loaded peer). Both v1 reference runs were stopped at the lead's request; the nosem comparison saved no rows. So the failure is already present at system 0 / representation level, before the generator: fixing R1 is the critical path.

## D-042 2026-09-25 packet-policy GRPO built; no signal from a non-competent base; on hold
GRPO track (research/tracks/grpo.md; merged on main): `rrp latent grpo` trains system i only (system 0, encoder and probes frozen), using an SDE sampler with exact per-step packet log-likelihoods, groups of 8 on the same seed, and a KL penalty to the reference (0.05, a labelled departure from Z-1). Reward = privileged sim success (reward only), with labelled optional shaping and a labelled teacher-prefix curriculum. Accounting is recorded, the target bodies are guarded (--allow-target), and system 0 is batched across rollouts. tests/unit/test_latent_grpo.py checks the likelihood math. Dev result (panda_pg2, 64 held-out dev seeds; base = flow_latent_sem_v2 snapshot @22k): 0/64 from reset before and after 15 iterations; with a 40-tick teacher prefix 0/64 → 2/64 → 1/64 → 1/64, i.e. no evidence of improvement, although the updates were healthy (KL 0.013 → 0.16, 114/120 informative groups). Budget: 960 training episodes, ~250k learned ticks, 942 updates, 4,431 s. Rollout failure modes, passed to the ladder track: tracking is fine; from reset the hand never approaches the cube (0.49–0.58 m); the gripper command sits at ~0.03 instead of the teacher's bimodal 0.045/0.0 (mode averaging); after a teacher handoff the policy pushes the cube and never lifts it; no packet rejections. On hold until a checkpoint succeeds well above 0% from reset.
Host incident: at 13:51–13:53 a few host job units failed at start with systemd status 219 ("Failed to create cgroup", ENOMEM). Not cgroup-ID exhaustion (258 memory cgroups, 89 dying). Leases started normally afterwards; treated as transient, watch for recurrence.

## D-043 2026-09-25 paired manipulator-assignment tasks delivered (item 2, arms); dual-arm training deferred
Dualarm track (research/tracks/dualarm.md; merged on main). New assign_left / assign_right pick-place: one layout per seed (bar and zone in the middle, reachable by both arms); the two task graphs differ only in which arm is bound to take/place. AssignedPickPlaceTeacher is labelled scripted_teacher: it uses privileged bar poses but reads the acting arm from the public task graph. Dataset assign_pick_place_v1: 4,665 successes / 293 failures / 2 infeasible. Pack assign_pick_place_v1_H16: 751,325 rows, 11 GB, role order (left, right). Pair index research/pairs/assign_pick_place_v1.json: 2,480 pairs with identical hashed initial states, 2,203 valid (source clean 1196/1200, source DART 929/1200, dev 60/60, held-out pair 18/20). Five invalid DART "pushed, not grasped" episodes are listed for filtering. Dual-arm latent code (M=2 role-ordered slots with stable handles and explicit null slots; per-arm probes with a slot-swap control; `rrp latent evaluate-dual --packet-edit swap_slots`) is merged and smoke-tested. Scripted-teacher reference on dev scenes: support_insert 20/20, 19/20, 20/20, held-out 9/17; handover 20/20 ×3, 16/16. No learned dual-arm model exists: the training chain was stopped at the re-scope (~300/8000 steps; ~16 h at the contended peer speed), and its 14 GB pack was deleted on the peer. The only remaining copy is on the host at ~/work/rrp-data/packed/dualarm_latent_v1_H16. Data caveats: burst DART breaks support_insert (94/680), 0.4% of handover rows lose touch tokens, and the ALOHA arm mapping is runtime only.
